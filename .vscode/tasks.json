{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            // 启动程序 (没有权限的话，将当前用户加入到docker组中)
            "taskName": "debug-start",
            "type": "shell",
            "command": "docker run -p 192.168.232.190:3306:3306 --rm -v ${workspaceRoot}/app:/app test"
        }
    ]
}