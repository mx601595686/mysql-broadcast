import io = require('socket.io');

export = class MysqlBroadcastClient {

    /**
     * 连接服务器
     * 
     * @static
     * @param {string} ip 服务器ip，默认localhost
     * @param {number} port 端口，默认3000
     */
    static start(ip: string = 'localhost', port: number = 3000) {
        
    }

    private constructor() {

    }
}